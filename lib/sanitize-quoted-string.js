/**
 * @fileoverview Shared quoted-string sanitizer (delegated to ergo-wire).
 *
 * @module lib/sanitize-quoted-string
 * @since 0.1.0
 */

export {sanitizeQuotedString as default} from '@centralping/ergo-wire/lib/quoted-string';
